name: flutter_esp
description: A new Flutter plugin project.
version: 0.1.0
homepage: https://github.com/060-dev/flutter_esp

environment:
   sdk: ^3.7.0
   flutter: ">=3.3.0"

dependencies:
   flutter:
      sdk: flutter
   plugin_platform_interface: ^2.0.2

dev_dependencies:
   flutter_test:
      sdk: flutter
   flutter_lints: ^5.0.0

flutter:
   plugin:
      platforms:
         android:
            package: dev.zsz.flutter_esp
            pluginClass: FlutterEspPlugin
         ios:
            pluginClass: FlutterEspPlugin
